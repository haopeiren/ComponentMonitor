<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffcs.mapper.ComponentConnectInfoMapper">

    <select id="getConnectionInfo" resultType="ComponentConnectInfo" parameterType="int">
        SELECT id, connect_type connectType, ip, port, username,
        password, component, `database`
        FROM components WHERE component = #{component}
    </select>
</mapper>
